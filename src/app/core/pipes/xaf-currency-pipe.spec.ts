import { XafCurrencyPipe } from './xaf-currency-pipe';

describe('XafCurrencyPipe', () => {
  it('create an instance', () => {
    const pipe = new XafCurrencyPipe();
    expect(pipe).toBeTruthy();
  });
});
